(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
'use strict';

var controllers = document.querySelector('.slider-contoler');
var firstSlide = document.querySelector('.first-slide');
var secondSlide = document.querySelector('.second-slide');
var allSiledes = [firstSlide, secondSlide];
var closeModal = document.querySelector('.modal-close');
var acc = document.querySelectorAll(".accordion");
var modal = document.querySelector('.custom-modal');
var openModal = document.querySelectorAll('.modal-open');
var burger = document.querySelector('.burger');
var nav = document.querySelector('#responsive');
var menu = document.querySelector('.menu-items');
var setOk = document.querySelector('.set-ok');

setTimeout(function () {
  return firstSlide.style.opacity = 1;
}, 500);

var header = document.querySelector('.header-main');

var animateItem = function animateItem(item) {
  setTimeout(function () {
    return item.style.opacity = 1;
  }, 5);
};

// controllers.addEventListener('click', (e) => {
//   let self = e.target;
//   if(self.tagName === 'I'){
//     for( let el of controllers.children ) {
//       el.classList.remove('active');
//     }
//     self.classList.add('active');
//     secondSlide.style.opacity = 0;
//     firstSlide.style.opacity = 0;

//     switch(self.getAttribute('data-slide')) {
//     case 'second':
//       secondSlide.classList.add('active-slide');
//       firstSlide.classList.remove('active-slide');
//       animateItem(secondSlide);
//       break;
//     case 'first':
//       firstSlide.classList.add('active-slide');
//       secondSlide.classList.remove('active-slide');
//       animateItem(firstSlide);
//       break;
//     }
//   }
// });

var OpenModal = function OpenModal() {
  modal.classList.remove('modal-hidden');
  modal.classList.add('modal-show');
};

openModal.forEach(function (element) {
  return element.addEventListener('click', OpenModal);
});

var countDownClock = function countDownClock() {
  var number = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 100;
  var format = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'seconds';


  var d = document;
  var daysElement = d.querySelector('.days');
  var hoursElement = d.querySelector('.hours');
  var minutesElement = d.querySelector('.minutes');
  var secondsElement = d.querySelector('.seconds');
  var countdown = void 0;
  convertFormat(format);

  function convertFormat(format) {
    switch (format) {
      case 'seconds':
        return timer(number);
      case 'minutes':
        return timer(number * 60);
      case 'hours':
        return timer(number * 60 * 60);
      case 'days':
        return timer(number * 60 * 60 * 24);
    }
  }

  function timer(seconds) {
    var now = Date.now();
    var then = now + seconds * 1000;

    countdown = setInterval(function () {
      var secondsLeft = Math.round((then - Date.now()) / 1000);

      if (secondsLeft <= 0) {
        clearInterval(countdown);
        return;
      };

      displayTimeLeft(secondsLeft);
    }, 1000);
  }

  function displayTimeLeft(seconds) {
    daysElement.textContent = '0' + Math.floor(seconds / 86400);
    hoursElement.textContent = '0' + Math.floor(seconds % 86400 / 3600);
    minutesElement.textContent = Math.floor(seconds % 86400 % 3600 / 60);
    secondsElement.textContent = seconds % 60 < 10 ? '0' + seconds % 60 : seconds % 60;
  }
};

/*
  start countdown
  enter number and format
  days, hours, 1minutes or seconds
*/
countDownClock(5, 'hours');

var CloseModal = function CloseModal() {
  modal.classList.remove('modal-show');
  modal.classList.add('modal-hidden');
};

$('#form').on('submit', function (e) {
  e.preventDefault();
  var fd = new FormData(this);
  $.ajax({
    url: 'send.php',
    type: 'POST',
    contentType: false,
    processData: false,
    data: fd,
    success: function success(msg) {
      if (msg === 'ok') {
        modal.classList.remove('modal-show');
        modal.classList.add('modal-hidden');

        setOk.classList.remove('hide-ok');
        setTimeout(function () {
          setOk.classList.add('hide-ok');
        }, 1500);
      } else {
        console.log(msg);
      }
    }
  });
});

$('.form-tarifi').on('submit', function (e) {
  e.preventDefault();
  var fd = new FormData(this);
  $.ajax({
    url: 'send.php',
    type: 'POST',
    contentType: false,
    processData: false,
    data: fd,
    success: function success(msg) {
      if (msg === 'ok') {
        modal.classList.remove('modal-show');
        modal.classList.add('modal-hidden');

        setOk.classList.remove('hide-ok');
        setTimeout(function () {
          setOk.classList.add('hide-ok');
        }, 1500);
      } else {
        console.log(msg);
      }
    }
  });
});

closeModal.addEventListener('click', CloseModal);

acc.forEach(function (el) {
  el.addEventListener('click', function (e) {
    var sibling = e.target.nextElementSibling;
    e.target.classList.toggle("active");
    if (sibling.style.maxHeight) {
      sibling.style.maxHeight = null;
    } else {
      sibling.style.maxHeight = sibling.scrollHeight + "px";
    }
  });
});

},{}]},{},[1])

//# sourceMappingURL=maps/app.js.map
